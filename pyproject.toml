[project]
name = "aapforge"
version = "0.1.0"
requires-python = ">=3.10"

[project.optional-dependencies]
extract = ["UnityPy>=1.10"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
