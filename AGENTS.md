# 本地协作规则

## 语言与术语

- 面向人的说明、文档、注释、错误信息和最终汇报，默认使用中文。
- 技术名词能用中文就用中文；确实需要保留英文时，必须同时给出中文说明。
- 不要让英文单词在正文里密集堆叠。必要的英文应当是代码名、协议名、文件名、第三方项目名或搜索/定位所需的原文。
- 固定机器契约不得强行翻译，例如 `.aap` 字段名、JSON 键、Python 标识符、测试函数名、命令参数、文件扩展名、第三方库名和已冻结的协议字符串。
- 第一次出现较生僻的英文专名时，写成“中文说明（英文原文）”。例如“契约（contract）”“黄金样例（golden fixture）”。

## 本仓库边界

- 当前仓库已经包含 M0：契约 / 数据冻结，M0.1：`.aap` 契约收紧，M1：源文件前端，以及 M1.1：源语言契约收紧和强类型规范中间表示。
- 后续开发必须按用户指定阶段推进，不得顺手越过当前阶段。
- 未经明确要求，不要实现解析器、语义校验器、写出器、安装器、素材库、进程管理或智能体技能。
- 维护已冻结的 AA 事实、HaloCue 已验证规则、最小 `.aap` 样例、源文件前端和离线测试时，要保持层次边界清楚。
- 正常运行不得依赖 HaloCue 运行时代码、AA 安装目录、用户素材、网络或个人配置。

## 提交信息风格

本仓库采用轻量的约定式提交（Conventional Commits）风格，结合中文说明。
参考来源包括约定式提交规范、Chris Beams 的提交信息七条规则、Angular
提交信息规范和 GitHub 上常见贡献指南。

- 提交标题格式：`类型(范围): 中文摘要`。范围可省略，例如 `feat: 实现源文件前端`。
- 类型使用固定英文短词，便于工具识别；摘要使用中文。
- 常用类型：
  - `feat`：新增能力。
  - `fix`：修复缺陷。
  - `test`：新增或调整测试。
  - `docs`：文档变更。
  - `refactor`：重构，不改变行为。
  - `chore`：维护性变更，例如忽略规则、配置、整理脚本。
- 标题尽量不超过 50 个字符，不以句号结尾。
- 一个提交只表达一组紧密相关的改动；不要把无关修复塞进同一个提交。
- 需要说明背景时，在标题后空一行写正文；正文说明“为什么改”和“影响什么”，不要复述代码细节。
- 正文每行尽量不超过 72 个字符。
- 破坏兼容时，在类型后加 `!`，并在正文写清影响，例如 `feat!: 调整源文件结构`。
- 提交前必须运行与改动范围匹配的测试，并确认没有缓存、用户路径、密钥、AA 资源或个人素材。
